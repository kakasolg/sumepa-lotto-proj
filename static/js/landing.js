// JavaScript specific to the landing page

// Example: Add a smooth scroll to anchor links if needed in the future
document.addEventListener('DOMContentLoaded', (event) => {
    // console.log("Landing page JS loaded");
});
